@model IEnumerable<string>
@{
    Layout = null;
}

@Html.ActionLink("Все товары","Index","Home")
@foreach(var link in Model)
{
    @Html.RouteLink(link, new { controller ="Home", action="Index",category=link, page=1},
    new { @class = link == ViewBag.SelectedCategory ? "selected" : null })
}
